# Token 速度后续修复与汇总 / ZCode 页面核对方案

> 交给实施模型执行，完成后交回当前任务审查。沿用 `TOKEN_SPEED_FIX_PLAN.md` 和基线 `1cd4a8e`。当前 V24 实现仍是未提交工作树；本文件是补充验收依据，不表示下述问题已经修好。实施前先读取完整当前 diff，不覆盖已有改动；未经用户另行要求，不提交、合并或推送。

## 一、已核对结论

1. 汇总页 ZCode 卡片读取 `stats.overall.avg_tps`（`src/SummaryTab.tsx`）；ZCode 详情页的平均速度与模型行读取同一 `stats`（`src/ZaiStatsContent.tsx`）。它们来自 ZCode **主 SQLite 库** `model_usage` 的 `db::query_stats`，不是另一个派生库，但**算法仍是旧算法**。所以两个页面彼此使用同一数值来源，却不等于速度排行 / 注入版 / 悬浮版的新口径。
2. `src-tauri/src/db.rs::speed_agg_columns` 用 `duration_ms - TTFT` 估算生成窗口，TTFT 占总耗时 ≥90% 时改用 TTFT；只收输出 ≥10、窗口 ≥100ms、速度 ≤500 t/s 的行，再对逐行速度做算术平均。新共享 `token_speed.rs` 使用真实 `first_token_at → completed_at`，无任意 100ms 下限或 500 t/s 封顶；`model_speed.rs` 的 `avg_tps` 是 `Σ合格输出 × 1000 / Σ合格生成毫秒`。这两个“平均速度”既不使用同一批样本，也不使用同一平均公式。
3. 本机 ZCode 主库只读匿名复核，同样以请求开始时间截取范围：近 24 小时旧算法约 **82.98 t/s**、旧样本 **2288**，真实生成区间合格样本 **1627** 的加权均速约 **63.10 t/s**；近 7 天旧算法约 **65.75 t/s**、旧样本 **10019**，真实生成区间合格样本 **7126** 的加权均速约 **60.20 t/s**。这证明口径差异具有可见量级；并非已经从当前运行 UI 截图核实的显示值。旧算法最高值被 ≤500 过滤截住，近 24 小时约 **480.43**；真实事件时间样本最高约 **1911.11**，后者也应在 UI 上明确是“单请求最高”、不是典型平均。异常高值要校验时间有效性，不能靠凭经验的硬上限默默丢弃。
4. `src/modelName.ts::foldModelStatRows` 在折叠同名模型时拿**所有输出 Token**给 `avg_tps` 做加权；这不是生成时长加权，且远端 Token 合并后会进一步污染权重。`src/merge.ts::mergeStats` 实际只保留**本机速度**，但汇总/ZCode UI 目前未标注“本机”，默认设备筛选为 `all`，容易让人理解成跨设备速度。
5. ZCode 模型行显示 `/s` 而非 `t/s`；汇总卡片仅写 `⚡ 数值 t/s`，不说“平均”或近似质量。最高速度与 TTFT 的说明目前主要藏在 `title`。本轮不得新增悬停卡片，也不得让必须理解的速度语义只靠悬停获得。中英切换要同步。
6. Claude 汇总速度来自仅有 `duration_ms` 的导入库；Kimi 的耗时由事件间隔估得，均无可靠首 Token → 完成的生成区间，不能与 ZCode 的可信生成速度不加区别地展示为同一种 `t/s`。Codex 无耗时不应编造速度。

注意对比条件：汇总/ZCode 页受设备筛选和今日/1 天/7 天/自定义范围影响，模型速度排行有自己的“今日/7 天”切换和 30 秒刷新。只有同一来源、同一 `[from,to)`、同一本机筛选、相同刷新时点，才能比较后台 `avg_tps`。排行里的“慢 / 平均 / 快”分别来自每请求速度分位数；其中“平均”目前是 p50（中位数），**不是**上述加权总体均速。不要拿 p50 与汇总均速做相等断言；如产品展示“平均”一词，须决定文案是否改为“典型”，并保持中英一致。

## 二、先修 V24 审查遗留的三项问题

### 1. 陈旧待处理消息不能永久冒充活跃轮

- `usage_feed.rs::collect_pending_user` 只查最后 64 条 user 消息，并用 7 天/3000 轮导出的 `done_user_msg_ids` 判断未匹配，没有时间新鲜度或终止条件。真实主库只读观察到约 12 小时未匹配消息。当前不一定已构成可见错误，但若后续无有效请求，可能长期显示 `– / 测速中`。
- 为“待处理消息”与“真实正在生成”分开建模。近期 user 消息可启动首请求等待；超出明确新鲜期后，若无可靠活跃证据应转为空闲/不可用；取消、失败、下一轮应立即清掉旧等待。不要简单给所有轮加 90 秒硬截止：真的耗时很长的首请求仍应保留等待，只需有可复核的活跃信号。
- 第一请求未完成时仍保留对应主会话及全层子代理的**全生命周期 `model_usage` 累计**；不能为了清陈旧速度状态把累计清零。给测试覆盖历史孤儿消息、长首请求、取消、新轮、多层子代理、老库缺少状态列。

### 2. 生成态不能由 `speedState == measuring` 反推

- `usage_feed.rs::SpeedView::from_rows` 当前把 `generating` 写成 `session.speed_state == Measuring`。一轮正在生成且已有一次完成请求时状态是 `Recent`；下一秒刷新若最新有效速度变为空，会错误转成 `Unavailable`。给会话传独立生成布尔量/可靠状态，不从“有没有速度”反推；状态机应允许“正在生成 + 本轮已完成请求有最近速度”。
- 测试“同轮第一请求完成 → 第二请求进行中 → 暂时没有有效速度 → 结束”与“新轮没有已确认速度”。注入/悬浮版都不能回填上一轮速度。

### 3. 一秒速度旁路必须真的小

- `usage-speed.js` 虽只含速度字段，当前 `SpeedView` 仍序列化全部历史 turns、runs、sessions；最多约 3000 轮时会达到数百 KiB，速度变更就重写全文件。不要以“字段少”代替文件大小/写入量验收。
- 历史轮/历史请求的稳定速度随原 2 秒历史数据发布；一秒旁路只发当前可见活跃会话/轮及必要状态与最新速度。定义历史数据与旁路数据的优先级、清空标记、升级兼容、注入页的查找方式，确保历史轮仍可显示已完成速度且新轮立即清旧值。若需要不兼容文件契约，显式升版本（V24 → 新版本）并测试已安装旧主题升级。
- 记录空闲、单活跃会话、3000 历史轮时 `usage-speed.js` 的字节数，以及一笔速度变化的实际写入量；确认 1 秒路径不触发 `usage-data.js` 大文件重写。不得以牺牲隐藏页现有降频、注入/悬浮开关独立性换取速度。

## 三、统一汇总、ZCode、模型速度排行的 ZCode 口径

### 1. 后台统计采用合格事件时间和可加总的分子/分母

- 给 ZCode 专门的生成速度聚合，或把数据源质量明确参数化；**不要直接全局替换** `db::speed_agg_columns` / `session_speed_agg_columns`，因为 Claude/Kimi 只有请求耗时，Codex 无耗时。读 `model_usage` 的 `status`（老库缺列按已完成表降级）、`started_at`、`first_token_at`、`completed_at`、`output_tokens`，用 `token_speed.rs` 的有效时间/完成状态规则验证。真实生成样本要求 `output_tokens > 0` 且 `completed_at > first_token_at > 0`；若首 Token 缺失，只能是 `request_average` 参考速度，不能静默混入 ZCode 的“生成均速”。未来时间、逆序、非终态、溢出、无效 span 返回无速度。合法短请求不可强制垫 100/1000ms，也不可因 >500 t/s 直接删除。
- `overall` 和每个 `(provider_id, model_id)` 同时积累 `speed_output_tokens`、`speed_generation_ms`、`speed_sample_count`、`max_tps`；`avg_tps = speed_output_tokens × 1000 / speed_generation_ms`，分母为零则 `null`，绝不显示 `0 t/s` 冒充缺失。若速度需要回退，另传 `speed_quality=request_average` 并显示 `≈`；更安全的 ZCode 历史页面方案是仅展示 generation 合格样本，缺失就 `—`，参考值不混算。
- 与 `model_speed.rs` 共用行级有效性判定。当前排行只判断 `completed > first > 0`，而共享核心还检查 `first >= started`、未来时间等；补齐并写共享测试。`avg_ttft_ms` 是单独指标，可有自己的有效样本数；不要假称它与速度样本完全同一集合。
- 审核 `projects.rs` 中 ZCode 项目/会话速度仍依赖旧 `session_speed_agg_columns` 的路径。要么让 ZCode 项目使用同一可加总生成口径，要么在汇报中清楚标为未统一的遗留；不可让后台函数局部变更后造成项目页静默错位。Claude/Kimi 项目保留各自源质量。

### 2. 修复同名模型折叠与多设备合并

- 同名模型折叠不能按 `m.output_tokens` 或 `m.avg_tps * output_tokens` 算速度。例：两个变体各输出 100 Token，生成时长分别 1s 与 0.1s，单行 100/1000 t/s；合并真实均速是 **200 / 1.1 = 181.8 t/s**，当前算法会得到 **550 t/s**。Rust `ModelStat/OverallStat`、`src/types.ts` 可增可选的合格输出、合格生成毫秒、合格样本数字段；`foldModelStatRows` 对分子/分母求和后再除，`max_tps` 取合格行最大。旧 JSON 缺字段时不能以全部 Token 伪造分母；选择不折叠速度显示 `—` 或明确兼容旧值的质量标记，并触发刷新。保住折叠幂等、变体明细、总 Token 和花费原逻辑。
- `mergeStats` 合并远端 Token/请求/花费时，速度分子、分母、样本数、TTFT 都只来自本机；远端仅有记录的模型不可凭远端输出构造速度。设备筛选 `all` 或指定远端时，汇总卡片、ZCode 详情卡、按模型行若显示本地速度，就可见标注“本机平均速度 / Local avg speed”；若设备筛选不包含本机，速度显示 `—` 或不渲染，不能给人一种远端速度已计算的印象。按模型折叠后的远端输出也不能改变本机 `avg_tps`。
- `DataCache.tsx` 的持久 `zbar-zai-cache` 无速度口径版本；升级新算法后可能短时显示旧缓存。做有版本的速度缓存迁移/针对性失效并自动刷新，尽量保留无关统计；旧缓存缺新增分子/分母时不能继续把旧 `avg_tps` 当成新 generation 口径。测试冷启动、本机/all/仅远端筛选、远端与本机同名模型、远端仅有模型。

### 3. 不同 Agent 的速度质量单独标识

- 汇总页也显示 Claude/Kimi 的速度，但这两种库没有 ZCode 的可信首 Token → 完成事件。若保留速度，按正输出和有效请求耗时计算**请求平均**，用独立的 `speed_quality=request_average` 和可见 `≈`，不标成“生成均速”。Kimi 耗时本身来自事件间隔，应再确认负值/跨请求间隔/重复事件的可靠性，不可因旧通用 SQL 能算出数值就宣传准确。Codex 无耗时保持无速度。
- 这一步要明确是否覆盖 Claude/Kimi 详情页与项目页；同一个数值不能在汇总页标 `≈`、详情页却仍无标记。若来源无法保证可靠，优先显示 `—`。历史数据质量不足时宁缺毋滥，不要拿 DOM 字符数或响应文本估算 Token。

## 四、页面展示与刷新验收

1. `SummaryTab.tsx` ZCode 卡片直接写“平均速度 63.1 t/s / Avg speed 63.1 t/s”，设备合并时写“本机平均速度 / Local avg speed”。Claude/Kimi 请求平均带 `≈`。数值旁无需新造“单位说明”；保留固定宽度或等宽数字，避免刷新抖动。
2. `ZaiStatsContent.tsx` 整体速度卡和模型行与同一 `stats` 同口径；模型行统一 `t/s`，不能只写 `/s`。无可信合格样本显示 `—`。`widgets.tsx` 中 `max_tps` 若继续提供“最快”，应能在页面上直接看到“单请求最快 X t/s / Fastest request X t/s”或删掉这个隐藏信息，别只靠 `title`，也不要混用 `tok/s` 与 `t/s`。
3. `AgentUsagePanel.tsx` Claude 模型行也有 `/s`，若 Claude 请求平均继续展示，应补 `≈` 和 `t/s`。新增文案通过 `src/i18n/dicts/*.ts` 与 `src/i18n/en.ts`，验证中文、英文及注入脚本的语言策略。不添加鼠标悬停卡片或悬停后才可读的解释。
4. `ModelSpeedPanel.tsx` 当前页面把 `requests` 写成“样本”，但生成速度合格样本比总请求少。单独提供并显示实际 `speed_sample_count`，总请求仍可显示为“请求数”；顶部“慢 / 平均 / 快”各有 `t/s`，说明它们来自分位数，但**不要新增 UI 解释卡**。如果保留“平均”作为 p50 文案，实施报告须说明它是中位数，与汇总页的总体均速不同；可考虑直接改为“典型 / Typical”以免误解，最终遵守用户不造冗余术语的偏好。
5. 今日/7 天等筛选和时间边界保持原交互；核对 `get_stats` 与 `get_model_speed` 的 `[from,to)`、本地时区和上限语义。注入/悬浮每秒节拍是**发现新完成请求**，不是逐 Token 流式速度；汇总/ZCode 的历史聚合不应每秒全库重算，沿用已有 DataCache 刷新并在 UI 标出统计范围/更新时间。测试切范围、新请求完成、设备筛选、中英切换和冷启动缓存。

## 五、实施顺序与验证

建议顺序：先补 V24 三项状态/性能问题 → 共用行级有效性与 ZCode 聚合 → 分子分母/合并/折叠/缓存 → Summary/ZCode/其他 Agent UI → 测试与匿名实机核对。修改每一层时检查所有消费端，不要仅改 SQL 后让 TS 折叠继续用错误权重。

必须有的自动用例：

- 一笔真实输出 1000 Token、首 Token 到完成 8s：可信 `125 t/s`，TTFT 再长也不应改成旧公式；合法 <100ms、>500 t/s 行不能仅因阈值被丢弃。逆序/未来/未完成/零输出无速度；首 Token 缺失只有明确 `≈` 请求平均或 `—`，老库缺列安全降级。
- 两个变体 100@1s 与 100@0.1s：整体与折叠后均速约 `181.8 t/s`，而不是 `550`；合并远端更多 Token 后本机速度仍不变。总体聚合与各模型可加总字段一致，折叠二次调用幂等。
- Summary 与 ZCode 详情在同一 DataCache Stats、同一范围下显示相同 ZCode 加权均速和质量；模型行含 `t/s`；本机/all/远端筛选标签正确，旧缓存升级后不冒充新公式；Claude/Kimi 参考速度均有 `≈`，Codex 无速度。
- 历史孤儿 user 消息不保持 Measuring；真实长首请求仍等待且会话历史 Token 不清零；同轮多请求、新轮、取消和 rollout 缺失时不复用旧速度。3000 历史轮场景的 1 秒旁路有可记录的文件字节数/写入量。
- 真实 UI：当前运行 ZCode 仍为 V22，须先在授权的测试环境安装/重启新主题才可能验证 V24/V25。匿名记录首请求未完成、请求完成、整轮完成三时点的 DB / 注入 / 悬浮累计和速度；仅注入、仅悬浮、同时开启均覆盖。不能向真实账号发起测试请求或更改安装主题而又把这类操作当成只读核对；无法实测时明确标“未验证”，不能写“通过”。

至少执行 `npm run build`、相关 `cargo test --lib`、注入脚本 JS 语法检查、`git diff --check`；有 `rustfmt` 时执行 `cargo fmt --check`。全库现有 `projects::tests::normalize_cwd_variants` Windows 路径断言失败要单列，不能把它算作本轮回归也不能谎称全库通过。补只读 ZCode 主库同范围旧/新结果审计，但不输出用户提示词、回复、会话 ID、凭证。所有数值比较记录 `[from,to)`、设备筛选、来源与刷新时间。

## 六、实施模型完成后必须汇报（供本任务审查）

1. **用户可见变化**：汇总、ZCode 详情、模型排行、注入、悬浮各展示什么速度；无速度、参考速度、生成速度及本机/远端分别怎样标记。
2. **逐文件改动清单**：每个文件及关键函数、数据契约/缓存版本变化、V24 三遗留各怎么处理；列出与本方案不同的决定与原因。
3. **公式与可加总字段**：行级有效性、总体及折叠分子/分母、样本数、`max_tps`、TTFT 独立口径，Claude/Kimi 的质量；明确仍无法做到逐 Token 实时测速。
4. **验证证据**：逐条实际命令与通过/失败/未运行；匿名只读近 24h、7d 的旧/新数值与相同过滤条件；同一范围 Summary/ZCode/排行后台 avg 的核对；中文/英文、三时点与三种开关组合的真实 UI 表；旁路字节数/写入量和缓存迁移测试。
5. **遗留风险**：私有 rollout 格式、未完成的真实安装核对、源库缺列、多设备缺耗时、现有全库失败、环境缺 rustfmt；任何未达验收项明确标出。
6. **审查定位**：基线 `1cd4a8e`、全部修改文件、`git status --short`、完整未提交 diff 范围；不要提交/合并/推送。不要把无关用户改动改掉。

完成汇报后等待当前任务审查；仅“构建通过”不足以证明页面速度口径正确。
