# 注入版与悬浮版 Token/速度一致性修复方案

> 交给修复模型实施，完成后交回原任务审查。本文是实施与验收依据；目前尚未修复代码。

## 目标与范围

1. 同一会话在两种界面中，已确认的 `↑/↓/⟲/Σ/请求数` 使用相同的数据库口径；请求落库并完成刷新后数值一致。
2. 主速度字段表示最近一笔**已完成模型请求**的平均输出速度。两种界面使用同一计算规则和数据质量标记，不用动画制造并不存在的实时测速。
3. 生成中尚无可确认用量时显示明确的等待状态；任何 DOM 文字估算都不能进入已确认的 Token 数量或冒充已测得的 `t/s`。
4. 保持注入版、悬浮版可以分别开启。界面文字适配中英切换，不新增鼠标悬停卡片。

本轮不修改 ZCode 的网络请求、流式协议或模型响应。现有 `model-io-*.jsonl` 每笔请求完成后才追加一行，不能提供逐 Token 的实时速率。若要真正流式、准确的 `t/s`，应另立任务确认是否有逐段 Token 计数或累计 usage 事件；仅有文本 chunk 仍是估算。

## 已复核的问题与代码入口

| 问题 | 位置 | 影响 |
| --- | --- | --- |
| 注入版每 200 ms 读取整个轮节点的 `textContent.length`，按 3.5 字符/Token 和约 1.5 秒窗口求速 | `src-tauri/src/agent_theme/inject.rs`：`dynTick`、`dynEstimate` | 中英混合、代码、思考文字、DOM 分批渲染会改变估算；短窗口使速度跳动 |
| `ensureRow` 把统计行追加到被采样的轮节点内，`syncDyn` 常在追加统计行前建立基线 | 同文件：`ensureRow`、`syncDyn`、`renderAll` | 统计行自己的文字可被误认为模型输出 |
| 每轮 live 行将 `r.out + estTok` 显示为 `↓`，但真实请求落库时没有推进估算基线 | 同文件：`liveLineOf`、`renderOne` | 一轮包含多笔请求时，已完成请求的可见文字可能与真实 `r.out` 重复计数；会话条真实累计没有这样叠加 |
| 注入会话累计由 `usage_feed.rs` 每 2 秒写 `usage-data.js`，页面每 2 秒重载；隐藏页降到 10 秒 | `usage_feed.rs`：`INTERVAL_MS`；`inject.rs`：`POLL_MS`、`POLL_HIDDEN_MS` | 同一数据库更新在注入页可比悬浮窗晚数秒；隐藏页更久。这是刷新延迟，不能当永久计数错误 |
| 悬浮版 rollout 解析只取 `startedAt` 与 `outputTokens`，用轮询时间当结束时间 | `src-tauri/src/session_hud.rs`：`parse_rollout_line`、`TreeSpeed::ingest` | 源记录已有 `completedAt`/`durationMs`，当前遗漏了精确结束时刻 |
| 悬浮版速率把耗时强制下限设为 1 秒，并把完成请求摊入 4 秒滑窗，窗口归零前不断衰减且可能保留衰减值 | 同文件：`amortized_rate`、`window_speed`、`display_speed` | 快请求被压低；请求结束后显示的速度变化并不代表新的 Token 到达 |
| 悬浮版会话树只收集直接子节点，注入版会话累计按 BFS 收集全部后代 | `session_hud.rs`：`collect_session_snapshot`；`usage_feed.rs`：`collect_session_stats` | 多层子代理可造成长期累计差异 |

本机只读验证：rollout 的实际记录包含 `startedAt`、`completedAt`、`durationMs`、`response.usage.outputTokens`，未见逐 chunk 用量字段。最近 24 小时样本中，2282 条 `completed` 请求都有正输出与请求耗时，1622 条有可用的 `first_token_at → completed_at`；其余 660 条需要降级显示。此比例只说明本机数据形态，不应写成产品常量。

## 统一数据契约与计算规则

先在 Rust 中定义可复用的会话树、已确认用量和速度计算。不要保留两套各自实现但名字相同的公式。

- **已确认累计**：每笔 `model_usage` 的非缓存输入 `max(input_tokens - cache_read_input_tokens, 0)`；`↓ = Σ output_tokens`，`⟲ = Σ cache_read_input_tokens`，`Σ = ↑ + ↓ + ⟲`，`请求数 = 行数`。主会话含所有层级子代理且每个成员只计一次。rollout 和 DOM 估算不进入这些合计。
- **可信生成速度**：仅在 `output_tokens > 0`、`first_token_at > 0`、`completed_at > first_token_at` 时，`t/s = output_tokens * 1000 / (completed_at - first_token_at)`。时间差为真实正值时，不再无条件垫到 1000 ms。
- **参考速度**：首 Token 时间不可用但 `duration_ms > 0` 时，可计算 `output_tokens * 1000 / duration_ms`；若以 rollout 先行提供参考速度，用其真实 `completedAt - startedAt`，必要时用可信的 `durationMs`。这包含首 Token 等待时间，必须带 `approximate`/`request_average` 质量标记，界面用 `≈` 区分，不能和可信生成速度无标记混用。缺失、逆序或无法验证的时间返回无值，不能制造 `0.0` 或沿用旧值。
- **请求优先级**：同一请求以有效 DB 首 Token/完成时间计算结果为准；rollout 只可在 DB 尚未可见时提前给出完成请求的参考值。合并时按稳定请求标识去重；若 DB 与 rollout 没有可靠的一对一标识，不要凭相近时间盲目合并，应明确采用一方为主并在汇报中说明。质量升级时允许一次修正，但不应循环抖动。
- **状态**：当前轮尚无完成请求 → `measuring`；有本轮已完成请求 → `recent`（最近已确认速度）；轮结束后保持最近有效值，但不得用上一轮的速度假装正在测当前轮。rollout 缺失时也要遵守新一轮清旧值的规则。区分 `null/暂无速度` 与真实数值 `0`。

建议给两种界面的速度快照提供 `sessionId`、`value|null`、`quality`、`completedAt`、可用的请求标识与本轮状态。界面应能知道一个值是生成阶段均速、包含等待的请求均速，还是尚在测量；不要只传一个裸 `number`。保持旧数据契约兼容，必要时添加版本/可选字段。

## 实施顺序

### 1. 先修错误计数

在注入版 live 每轮行中，`↓` 只显示数据库已确认输出；删除 `r.out + estTok` 这种混算。若保留可见文本估算，必须独立显示并标 `≈`，且说明它不是“尚未落库的 Token”；最简洁的方案是暂时不展示估算 Token。顶部会话条的 `Σ/↑/↓/⟲/×` 继续只显示真实值。同步修订这些分支的注释与旧字符串断言测试。

若仍保留 DOM 估算作为**单独的辅助速度**，采样必须排除 `[data-zbar-usage-row]`、脚本自身生成的元素和非回复 UI，并处理节点重建/换轮/页面批量刷新；速度字段前加 `≈`，无足够样本时显示占位，不得显示 `0.0 t/s`。更推荐本轮把主速度统一到已完成请求速度，生成中的即时估算不作为主指标。

### 2. 抽取共用的会话树与速度核心

把主会话及任意深度子代理的成员解析放在 Rust 共用函数中，供 `usage_feed.rs` 与 `session_hud.rs` 调用；成员去重、防父子环、老库缺少 `parent_id` 的降级均需保留。两端总量查询仍可按各自窗口选择展示会话，但**同一可见会话的全生命周期累计**必须用同一成员集合和公式。

提取请求速度的纯函数和可测试解析器。rollout 解析增加 `completedAt`、`durationMs`（可选字段兼容私有格式变化），校验 `outputTokens` 和时间；不再用本地轮询时刻冒充完成时刻。把悬浮版的 4 秒加权滑窗及其“归零时保持最后非零”逻辑改为真实的最近完成请求均速；每秒节拍只负责发现新请求，不负责让已有请求的速度自行衰减。`model_usage` 首 Token 时间具备时升级为可信速度。

### 3. 向两种界面供数

悬浮版延续现有 Tauri 事件；注入版可使用独立、很小的速度数据文件（原子替换、版本字段、内容变化才写），不要为 1 秒速度节拍每次重写几百 KB 的 `usage-data.js`。二者调用同一核心计算/状态机。若改成独立共用工作线程，生命周期条件为“注入皮肤已安装 **或** 悬浮版开启”，两者都关闭时停止；若保留两个工作线程，也必须共用纯函数和同一契约。特别测试单独只开任意一种界面、同时开启、关闭再打开。

主库累计允许现有约 2 秒轮询；注入页面多一层文件加载会带来短暂延迟。将“源数据落库/文件追加到可见界面”的延迟作为单独指标测量，在页面可见、源文件正常的前提下目标为约 2 秒内更新速度。隐藏页面已有 10 秒降频策略，不要把隐藏页的延迟误诊为计数公式错误。

### 4. 界面与兼容性

两端 `t/s` 只属于同一种速度语义；参考值显示 `≈`，等待态显示 `–` 或直接可见的“测速中 / Measuring”。任何新增文字必须适配现有中英切换；注入进 ZCode 的脚本若拿不到本软件的语言偏好，可用语言无关的 `–`/`≈`，不要写死中文。不要新增 hover 卡片或依赖 tooltip 才能看懂口径。保留数字等宽、固定字段布局，避免频繁变化引发宽度抖动。

## 必须补的验证

1. **纯函数/解析测试**：有/无 `completedAt`、`durationMs`、首 Token 时间，逆序/未来时间，短于 1 秒的合法请求，零/负输出，半行、截断重建、重复请求；确认不再用轮询时刻和 1 秒下限扭曲合法速率。
2. **状态测试**：首轮开始、请求完成、同一轮多次请求、轮结束、新轮开始、rollout 缺失、DB 晚于 rollout、子代理并发、窗口重开；旧速度不冒充当前速度，已完成请求速率不会按 4 秒窗口自行衰减。
3. **计数测试**：注入统计行本身不进入任何保留的 DOM 采样；一轮多次请求时 `↓` 不重复计数；有两层以上子代理时两个界面汇总相等；老库缺列仍降级。旧的“应含估算叠加/4 秒滑窗”断言应替换成行为测试，不能只改断言文本。
4. **真实界面核对**：同一匿名会话分别记录数据库、注入会话条/每轮行、悬浮窗的 `↑/↓/Σ/×`，在首请求未完成、请求刚完成、整轮完成各观察一次；同时覆盖中文、英文、代码长输出、工具调用、隐藏再显示、仅开一个界面、中英切换。不要把真实提示词、回复内容或凭证写进测试夹具和汇报。
5. **构建检查**：从仓库根运行 `npm run build`；从 `src-tauri` 运行 `cargo fmt --check` 与相关 `cargo test --lib`；最后运行 `git diff --check`。有环境导致无法运行的命令要报告命令、错误和未验证范围，不能写“测试通过”。

示例验收：一笔请求真实输出 1000 Token，首 Token 到完成耗时 8 秒，则可信生成均速为 `125.0 t/s`；请求结束后不得自动变成约 `93.8/62.5/31.3 t/s`。一轮先落库 100 Token、后续请求尚在流式输出时，每轮 `↓` 不得把已落库的 100 再与对应 DOM 文本估算相加。源数据稳定后，同一会话两界面已确认累计必须与数据库一致。

## 修复模型完成后的交付汇报（必须填写）

请在交回审查时按下面格式逐项汇报，不要只说“已修复”：

1. **变更摘要**：用户实际看到的前后行为；特别说明生成中与请求完成后的 `t/s` 各表示什么。
2. **改动清单**：每个修改文件、关键函数、改动目的；列明移除的旧滑窗/估算叠加和新增的数据契约。
3. **口径与降级**：真实累计的会话树范围、首 Token 缺失时如何算速度、rollout 与 DB 的优先级/去重方式、新轮如何清除旧值。若与本方案不同，逐项说明原因和影响。
4. **验证证据**：实际执行的命令与结果；关键自动测试名称；匿名实机核对的“DB / 注入 / 悬浮”数值和观察时点；未覆盖的场景。
5. **风险与遗留**：仍属于估算的指标、私有 rollout 格式变化时的行为、刷新延迟、任何已知不一致；如未实现某条验收标准，明确标出。
6. **审查定位**：基线 commit、最终 commit（若已提交）或未提交 diff 范围，以及 `git status --short`。不要把无关改动混进来；审查前不要合并或推送。

审查将依据上述口径、代码差异、测试与真实界面结果判断；仅有截图或“测试通过”的一句话不足以确认速度准确性。
